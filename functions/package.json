{
  "name": "public-speech-feedback-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "storage:videos:scan": "node scripts/download-storage-videos.js --dry-run",
    "storage:videos:download": "node scripts/download-storage-videos.js --download",
    "storage:videos:attach-firestore": "node scripts/attach-video-firestore-metadata.js",
    "storage:videos:attach-pdfs": "node scripts/attach-video-pdfs.js",
    "storage:videos:organize": "node scripts/organize-video-bundles.js"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1",
    "google-auth-library": "^9.15.0"
  }
}
